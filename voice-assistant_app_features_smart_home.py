import requests

def control_device(device_name, action):
    # Placeholder function for controlling smart home devices
    # In a real implementation, this would involve sending an API request to the smart home system
    response = f"{action.capitalize()} {device_name}"
    return response